services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: property_ai
      POSTGRES_PASSWORD: property_ai
      POSTGRES_DB: property_ai_workspace
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  postgres_data:
